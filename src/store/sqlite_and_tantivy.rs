// This file define the database using sqlx + SQLite and implements Tantivy for full-text search. It provides functions to store and retrieve emails, as well as to perform full-text search on email content. The module implements the necessary database schema, connection management, and search indexing as defined in database.rs so that it is compatible with the rest of the agent's storage interface. It also includes functions to initialize the database and search index, as well as to perform search queries efficiently.
